
const roleProvider = require('./role');
const areaProvider = require('./area');
const clusterProvider = require('./cluster');
const clubProvider = require('./club');
const userProvider = require('./user');
const personCardProvider = require('./personcard');
const eventProvider = require('./event');
const messageProvider = require('./message');
const menuPageProvider = require('./menupage');
const utilProvider = require('./util');

module.exports = {
    roleProvider,
    areaProvider,
    clusterProvider,
    clubProvider,
    userProvider,
    personCardProvider,
    eventProvider,
    messageProvider,
    menuPageProvider,
    utilProvider
};
